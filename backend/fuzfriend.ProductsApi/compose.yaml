services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: EcommerceDb
    volumes:
      - pg_data:/var/lib/postgresql
    ports:
      - "5432:5432"

volumes:
  pg_data: